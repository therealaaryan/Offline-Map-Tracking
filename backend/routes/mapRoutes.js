const express = require('express');
const router = express.Router();

// Example route (you can remove this once you add actual routes)
router.get('/test', (req, res) => {
  res.send('Map route test');
});

module.exports = router;
